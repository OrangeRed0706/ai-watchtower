{
  "name": "ai-watchtower",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "ingest": "node scripts/ingest.js",
    "build": "eleventy",
    "dev": "eleventy --serve --watch",
    "clean": "rm -rf public .cache"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
